const INITIAL_STATE = {
  loading: false,
  stock: [],
  error: '',
};

const mainPageReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mainPageReducer;
