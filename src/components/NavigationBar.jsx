import React from "react";

const NavigationBar = ({ currentPage }) => {
  return (
    <div>
      <Link to="/main">
        Página Inicial
      </Link>
    </div>
  );
};

export default NavigationBar;
